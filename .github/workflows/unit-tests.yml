name: Unit Tests & Security Scan

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  test-agendaSoa:
    name: agendaSoa
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: agendaSoa
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: agendaSoa/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Security scan
        run: |
          mkdir -p coverage
          npm audit --json > coverage/audit-report.json 2>&1 || true
          npm audit --audit-level=high && touch .scan-ok || true
      - name: Run tests with coverage (≥90%)
        run: npm run test:coverage -- --verbose
      - name: Generate step summary
        if: always()
        run: |
          {
            # ── Security Scan ─────────────────────────────────────────
            echo "## 🔒 Security Scan"
            if [ -f .scan-ok ]; then
              echo "✅ Nessuna vulnerabilità high/critical trovata"
            else
              echo "❌ Vulnerabilità high/critical rilevate"
            fi
            rm -f .scan-ok
            if [ -f coverage/audit-report.json ]; then
              node << 'AUDEOF'
          try {
            const a = require('./coverage/audit-report.json');
            const v = a.metadata?.vulnerabilities || {};
            const total = (v.info||0)+(v.low||0)+(v.moderate||0)+(v.high||0)+(v.critical||0);
            if (total > 0) {
              console.log('');
              console.log('| Severity | Conteggio |');
              console.log('|---|---|');
              if (v.critical) console.log('| 🔴 Critical | ' + v.critical + ' |');
              if (v.high)     console.log('| 🟠 High     | ' + v.high + ' |');
              if (v.moderate) console.log('| 🟡 Moderate | ' + v.moderate + ' |');
              if (v.low)      console.log('| 🟢 Low      | ' + v.low + ' |');
              if (v.info)     console.log('| ℹ️ Info    | ' + v.info + ' |');
              const highs = Object.values(a.vulnerabilities||{}).filter(p=>['high','critical'].includes(p.severity));
              if (highs.length) {
                console.log('');
                console.log('<details><summary>Dettaglio vulnerabilità high/critical</summary>');
                console.log('');
                console.log('| Package | Severity | Problema | Fix disponibile |');
                console.log('|---|---|---|---|');
                for (const p of highs) {
                  const via = (p.via||[]).map(v=>typeof v==='string'?v:(v.title||v.name||'?')).join(', ');
                  const fix = p.fixAvailable===true ? '✅ sì' : p.fixAvailable?.name ? '⚠️ breaking: '+p.fixAvailable.name : '❌ no';
                  console.log('| `'+p.name+'` | **'+p.severity+'** | '+via+' | '+fix+' |');
                }
                console.log('');
                console.log('</details>');
              }
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere audit-report.json: ' + e.message); }
          AUDEOF
            fi
            # ── Test Results ──────────────────────────────────────────
            if [ -f coverage/jest-results.json ]; then
              echo ""
              echo "## 🧪 Test Results — $(basename $PWD)"
              node << 'TSTEOF'
          try {
            const r = require('./coverage/jest-results.json');
            const ok='✅', ko='❌', sk='⏭️';
            console.log('| | Totale |');
            console.log('|---|---|');
            console.log('| ✅ Passed  | **' + r.numPassedTests  + '** |');
            if (r.numFailedTests)  console.log('| ❌ Failed  | **' + r.numFailedTests  + '** |');
            if (r.numPendingTests) console.log('| ⏭️ Skipped | **' + r.numPendingTests + '** |');
            console.log('| ⏱️ Durata  | ' + (r.testResults?.reduce((a,s)=>a+(s.endTime-s.startTime),0)/1000).toFixed(2) + 's |');
            const failed = (r.testResults||[]).flatMap(s=>(s.testResults||[]).filter(t=>t.status==='failed'));
            if (failed.length) {
              console.log('');
              console.log('<details><summary>❌ Test falliti</summary>');
              console.log('');
              for (const t of failed) {
                console.log('**' + t.ancestorTitles.concat(t.title).join(' › ') + '**');
                console.log('');
                console.log('```');
                console.log((t.failureMessages||[]).join('\n').substring(0,500));
                console.log('```');
                console.log('');
              }
              console.log('</details>');
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere jest-results.json: ' + e.message); }
          TSTEOF
            fi
            # ── Coverage ──────────────────────────────────────────────
            if [ -f coverage/coverage-summary.json ]; then
              echo ""
              echo "## 📊 Coverage — $(basename $PWD)"
              node << 'COVEOF'
          try {
            const s = require('./coverage/coverage-summary.json');
            const ok='✅', ko='❌';
            const fmt  = m => (m.pct>=90?ok:ko)+' **'+m.pct+'%**';
            const fmt2 = m => (m.pct>=90?ok:ko)+' '+m.pct+'%';
            const t = s.total;
            console.log('### Totale');
            console.log('| Metrica | Copertura | Covered / Total |');
            console.log('|---|---|---|');
            for (const [k,m] of Object.entries(t)) {
              console.log('| '+k+' | '+fmt(m)+' | '+m.covered+' / '+m.total+' |');
            }
            const files = Object.entries(s).filter(([k])=>k!=='total');
            if (files.length) {
              console.log('');
              console.log('<details><summary>📁 Dettaglio per file</summary>');
              console.log('');
              console.log('| File | Stmt | Branch | Func | Lines |');
              console.log('|---|---|---|---|---|');
              for (const [file,m] of files) {
                const name = file.split('/').pop();
                console.log('| `'+name+'` | '+fmt2(m.statements)+' | '+fmt2(m.branches)+' | '+fmt2(m.functions)+' | '+fmt2(m.lines)+' |');
              }
              console.log('');
              console.log('</details>');
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere coverage-summary.json: ' + e.message); }
          COVEOF
            else
              echo ""
              echo "## 📊 Coverage"
              echo "❌ Report non disponibile — test falliti o non eseguiti"
            fi
            echo ""
            echo "> 📁 Scarica l\'artefatto **coverage-$(basename $PWD)** per: HTML coverage (lcov-report/index.html), report test (test-report.html), dettaglio audit (audit-report.json)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: coverage-agendaSoa
          path: agendaSoa/coverage/
          retention-days: 30

  test-agendaSoaNaga:
    name: agendaSoaNaga
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: agendaSoaNaga
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: agendaSoaNaga/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Security scan
        run: |
          mkdir -p coverage
          npm audit --json > coverage/audit-report.json 2>&1 || true
          npm audit --audit-level=high && touch .scan-ok || true
      - name: Run tests with coverage (≥90%)
        run: npm run test:coverage -- --verbose
      - name: Generate step summary
        if: always()
        run: |
          {
            # ── Security Scan ─────────────────────────────────────────
            echo "## 🔒 Security Scan"
            if [ -f .scan-ok ]; then
              echo "✅ Nessuna vulnerabilità high/critical trovata"
            else
              echo "❌ Vulnerabilità high/critical rilevate"
            fi
            rm -f .scan-ok
            if [ -f coverage/audit-report.json ]; then
              node << 'AUDEOF'
          try {
            const a = require('./coverage/audit-report.json');
            const v = a.metadata?.vulnerabilities || {};
            const total = (v.info||0)+(v.low||0)+(v.moderate||0)+(v.high||0)+(v.critical||0);
            if (total > 0) {
              console.log('');
              console.log('| Severity | Conteggio |');
              console.log('|---|---|');
              if (v.critical) console.log('| 🔴 Critical | ' + v.critical + ' |');
              if (v.high)     console.log('| 🟠 High     | ' + v.high + ' |');
              if (v.moderate) console.log('| 🟡 Moderate | ' + v.moderate + ' |');
              if (v.low)      console.log('| 🟢 Low      | ' + v.low + ' |');
              if (v.info)     console.log('| ℹ️ Info    | ' + v.info + ' |');
              const highs = Object.values(a.vulnerabilities||{}).filter(p=>['high','critical'].includes(p.severity));
              if (highs.length) {
                console.log('');
                console.log('<details><summary>Dettaglio vulnerabilità high/critical</summary>');
                console.log('');
                console.log('| Package | Severity | Problema | Fix disponibile |');
                console.log('|---|---|---|---|');
                for (const p of highs) {
                  const via = (p.via||[]).map(v=>typeof v==='string'?v:(v.title||v.name||'?')).join(', ');
                  const fix = p.fixAvailable===true ? '✅ sì' : p.fixAvailable?.name ? '⚠️ breaking: '+p.fixAvailable.name : '❌ no';
                  console.log('| `'+p.name+'` | **'+p.severity+'** | '+via+' | '+fix+' |');
                }
                console.log('');
                console.log('</details>');
              }
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere audit-report.json: ' + e.message); }
          AUDEOF
            fi
            # ── Test Results ──────────────────────────────────────────
            if [ -f coverage/jest-results.json ]; then
              echo ""
              echo "## 🧪 Test Results — $(basename $PWD)"
              node << 'TSTEOF'
          try {
            const r = require('./coverage/jest-results.json');
            const ok='✅', ko='❌', sk='⏭️';
            console.log('| | Totale |');
            console.log('|---|---|');
            console.log('| ✅ Passed  | **' + r.numPassedTests  + '** |');
            if (r.numFailedTests)  console.log('| ❌ Failed  | **' + r.numFailedTests  + '** |');
            if (r.numPendingTests) console.log('| ⏭️ Skipped | **' + r.numPendingTests + '** |');
            console.log('| ⏱️ Durata  | ' + (r.testResults?.reduce((a,s)=>a+(s.endTime-s.startTime),0)/1000).toFixed(2) + 's |');
            const failed = (r.testResults||[]).flatMap(s=>(s.testResults||[]).filter(t=>t.status==='failed'));
            if (failed.length) {
              console.log('');
              console.log('<details><summary>❌ Test falliti</summary>');
              console.log('');
              for (const t of failed) {
                console.log('**' + t.ancestorTitles.concat(t.title).join(' › ') + '**');
                console.log('');
                console.log('```');
                console.log((t.failureMessages||[]).join('\n').substring(0,500));
                console.log('```');
                console.log('');
              }
              console.log('</details>');
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere jest-results.json: ' + e.message); }
          TSTEOF
            fi
            # ── Coverage ──────────────────────────────────────────────
            if [ -f coverage/coverage-summary.json ]; then
              echo ""
              echo "## 📊 Coverage — $(basename $PWD)"
              node << 'COVEOF'
          try {
            const s = require('./coverage/coverage-summary.json');
            const ok='✅', ko='❌';
            const fmt  = m => (m.pct>=90?ok:ko)+' **'+m.pct+'%**';
            const fmt2 = m => (m.pct>=90?ok:ko)+' '+m.pct+'%';
            const t = s.total;
            console.log('### Totale');
            console.log('| Metrica | Copertura | Covered / Total |');
            console.log('|---|---|---|');
            for (const [k,m] of Object.entries(t)) {
              console.log('| '+k+' | '+fmt(m)+' | '+m.covered+' / '+m.total+' |');
            }
            const files = Object.entries(s).filter(([k])=>k!=='total');
            if (files.length) {
              console.log('');
              console.log('<details><summary>📁 Dettaglio per file</summary>');
              console.log('');
              console.log('| File | Stmt | Branch | Func | Lines |');
              console.log('|---|---|---|---|---|');
              for (const [file,m] of files) {
                const name = file.split('/').pop();
                console.log('| `'+name+'` | '+fmt2(m.statements)+' | '+fmt2(m.branches)+' | '+fmt2(m.functions)+' | '+fmt2(m.lines)+' |');
              }
              console.log('');
              console.log('</details>');
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere coverage-summary.json: ' + e.message); }
          COVEOF
            else
              echo ""
              echo "## 📊 Coverage"
              echo "❌ Report non disponibile — test falliti o non eseguiti"
            fi
            echo ""
            echo "> 📁 Scarica l\'artefatto **coverage-$(basename $PWD)** per: HTML coverage (lcov-report/index.html), report test (test-report.html), dettaglio audit (audit-report.json)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: coverage-agendaSoaNaga
          path: agendaSoaNaga/coverage/
          retention-days: 30

  test-dms:
    name: dms
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: dms
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: dms/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Security scan
        run: |
          mkdir -p coverage
          npm audit --json > coverage/audit-report.json 2>&1 || true
          npm audit --audit-level=high && touch .scan-ok || true
      - name: Run tests with coverage (≥90%)
        run: npm run test:coverage -- --verbose
      - name: Generate step summary
        if: always()
        run: |
          {
            # ── Security Scan ─────────────────────────────────────────
            echo "## 🔒 Security Scan"
            if [ -f .scan-ok ]; then
              echo "✅ Nessuna vulnerabilità high/critical trovata"
            else
              echo "❌ Vulnerabilità high/critical rilevate"
            fi
            rm -f .scan-ok
            if [ -f coverage/audit-report.json ]; then
              node << 'AUDEOF'
          try {
            const a = require('./coverage/audit-report.json');
            const v = a.metadata?.vulnerabilities || {};
            const total = (v.info||0)+(v.low||0)+(v.moderate||0)+(v.high||0)+(v.critical||0);
            if (total > 0) {
              console.log('');
              console.log('| Severity | Conteggio |');
              console.log('|---|---|');
              if (v.critical) console.log('| 🔴 Critical | ' + v.critical + ' |');
              if (v.high)     console.log('| 🟠 High     | ' + v.high + ' |');
              if (v.moderate) console.log('| 🟡 Moderate | ' + v.moderate + ' |');
              if (v.low)      console.log('| 🟢 Low      | ' + v.low + ' |');
              if (v.info)     console.log('| ℹ️ Info    | ' + v.info + ' |');
              const highs = Object.values(a.vulnerabilities||{}).filter(p=>['high','critical'].includes(p.severity));
              if (highs.length) {
                console.log('');
                console.log('<details><summary>Dettaglio vulnerabilità high/critical</summary>');
                console.log('');
                console.log('| Package | Severity | Problema | Fix disponibile |');
                console.log('|---|---|---|---|');
                for (const p of highs) {
                  const via = (p.via||[]).map(v=>typeof v==='string'?v:(v.title||v.name||'?')).join(', ');
                  const fix = p.fixAvailable===true ? '✅ sì' : p.fixAvailable?.name ? '⚠️ breaking: '+p.fixAvailable.name : '❌ no';
                  console.log('| `'+p.name+'` | **'+p.severity+'** | '+via+' | '+fix+' |');
                }
                console.log('');
                console.log('</details>');
              }
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere audit-report.json: ' + e.message); }
          AUDEOF
            fi
            # ── Test Results ──────────────────────────────────────────
            if [ -f coverage/jest-results.json ]; then
              echo ""
              echo "## 🧪 Test Results — $(basename $PWD)"
              node << 'TSTEOF'
          try {
            const r = require('./coverage/jest-results.json');
            const ok='✅', ko='❌', sk='⏭️';
            console.log('| | Totale |');
            console.log('|---|---|');
            console.log('| ✅ Passed  | **' + r.numPassedTests  + '** |');
            if (r.numFailedTests)  console.log('| ❌ Failed  | **' + r.numFailedTests  + '** |');
            if (r.numPendingTests) console.log('| ⏭️ Skipped | **' + r.numPendingTests + '** |');
            console.log('| ⏱️ Durata  | ' + (r.testResults?.reduce((a,s)=>a+(s.endTime-s.startTime),0)/1000).toFixed(2) + 's |');
            const failed = (r.testResults||[]).flatMap(s=>(s.testResults||[]).filter(t=>t.status==='failed'));
            if (failed.length) {
              console.log('');
              console.log('<details><summary>❌ Test falliti</summary>');
              console.log('');
              for (const t of failed) {
                console.log('**' + t.ancestorTitles.concat(t.title).join(' › ') + '**');
                console.log('');
                console.log('```');
                console.log((t.failureMessages||[]).join('\n').substring(0,500));
                console.log('```');
                console.log('');
              }
              console.log('</details>');
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere jest-results.json: ' + e.message); }
          TSTEOF
            fi
            # ── Coverage ──────────────────────────────────────────────
            if [ -f coverage/coverage-summary.json ]; then
              echo ""
              echo "## 📊 Coverage — $(basename $PWD)"
              node << 'COVEOF'
          try {
            const s = require('./coverage/coverage-summary.json');
            const ok='✅', ko='❌';
            const fmt  = m => (m.pct>=90?ok:ko)+' **'+m.pct+'%**';
            const fmt2 = m => (m.pct>=90?ok:ko)+' '+m.pct+'%';
            const t = s.total;
            console.log('### Totale');
            console.log('| Metrica | Copertura | Covered / Total |');
            console.log('|---|---|---|');
            for (const [k,m] of Object.entries(t)) {
              console.log('| '+k+' | '+fmt(m)+' | '+m.covered+' / '+m.total+' |');
            }
            const files = Object.entries(s).filter(([k])=>k!=='total');
            if (files.length) {
              console.log('');
              console.log('<details><summary>📁 Dettaglio per file</summary>');
              console.log('');
              console.log('| File | Stmt | Branch | Func | Lines |');
              console.log('|---|---|---|---|---|');
              for (const [file,m] of files) {
                const name = file.split('/').pop();
                console.log('| `'+name+'` | '+fmt2(m.statements)+' | '+fmt2(m.branches)+' | '+fmt2(m.functions)+' | '+fmt2(m.lines)+' |');
              }
              console.log('');
              console.log('</details>');
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere coverage-summary.json: ' + e.message); }
          COVEOF
            else
              echo ""
              echo "## 📊 Coverage"
              echo "❌ Report non disponibile — test falliti o non eseguiti"
            fi
            echo ""
            echo "> 📁 Scarica l\'artefatto **coverage-$(basename $PWD)** per: HTML coverage (lcov-report/index.html), report test (test-report.html), dettaglio audit (audit-report.json)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: coverage-dms
          path: dms/coverage/
          retention-days: 30

  test-jobcard:
    name: jobcard
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: jobcard
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: jobcard/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Security scan
        run: |
          mkdir -p coverage
          npm audit --json > coverage/audit-report.json 2>&1 || true
          npm audit --audit-level=high && touch .scan-ok || true
      - name: Run tests with coverage (≥90%)
        run: npm run test:coverage -- --verbose
      - name: Generate step summary
        if: always()
        run: |
          {
            # ── Security Scan ─────────────────────────────────────────
            echo "## 🔒 Security Scan"
            if [ -f .scan-ok ]; then
              echo "✅ Nessuna vulnerabilità high/critical trovata"
            else
              echo "❌ Vulnerabilità high/critical rilevate"
            fi
            rm -f .scan-ok
            if [ -f coverage/audit-report.json ]; then
              node << 'AUDEOF'
          try {
            const a = require('./coverage/audit-report.json');
            const v = a.metadata?.vulnerabilities || {};
            const total = (v.info||0)+(v.low||0)+(v.moderate||0)+(v.high||0)+(v.critical||0);
            if (total > 0) {
              console.log('');
              console.log('| Severity | Conteggio |');
              console.log('|---|---|');
              if (v.critical) console.log('| 🔴 Critical | ' + v.critical + ' |');
              if (v.high)     console.log('| 🟠 High     | ' + v.high + ' |');
              if (v.moderate) console.log('| 🟡 Moderate | ' + v.moderate + ' |');
              if (v.low)      console.log('| 🟢 Low      | ' + v.low + ' |');
              if (v.info)     console.log('| ℹ️ Info    | ' + v.info + ' |');
              const highs = Object.values(a.vulnerabilities||{}).filter(p=>['high','critical'].includes(p.severity));
              if (highs.length) {
                console.log('');
                console.log('<details><summary>Dettaglio vulnerabilità high/critical</summary>');
                console.log('');
                console.log('| Package | Severity | Problema | Fix disponibile |');
                console.log('|---|---|---|---|');
                for (const p of highs) {
                  const via = (p.via||[]).map(v=>typeof v==='string'?v:(v.title||v.name||'?')).join(', ');
                  const fix = p.fixAvailable===true ? '✅ sì' : p.fixAvailable?.name ? '⚠️ breaking: '+p.fixAvailable.name : '❌ no';
                  console.log('| `'+p.name+'` | **'+p.severity+'** | '+via+' | '+fix+' |');
                }
                console.log('');
                console.log('</details>');
              }
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere audit-report.json: ' + e.message); }
          AUDEOF
            fi
            # ── Test Results ──────────────────────────────────────────
            if [ -f coverage/jest-results.json ]; then
              echo ""
              echo "## 🧪 Test Results — $(basename $PWD)"
              node << 'TSTEOF'
          try {
            const r = require('./coverage/jest-results.json');
            const ok='✅', ko='❌', sk='⏭️';
            console.log('| | Totale |');
            console.log('|---|---|');
            console.log('| ✅ Passed  | **' + r.numPassedTests  + '** |');
            if (r.numFailedTests)  console.log('| ❌ Failed  | **' + r.numFailedTests  + '** |');
            if (r.numPendingTests) console.log('| ⏭️ Skipped | **' + r.numPendingTests + '** |');
            console.log('| ⏱️ Durata  | ' + (r.testResults?.reduce((a,s)=>a+(s.endTime-s.startTime),0)/1000).toFixed(2) + 's |');
            const failed = (r.testResults||[]).flatMap(s=>(s.testResults||[]).filter(t=>t.status==='failed'));
            if (failed.length) {
              console.log('');
              console.log('<details><summary>❌ Test falliti</summary>');
              console.log('');
              for (const t of failed) {
                console.log('**' + t.ancestorTitles.concat(t.title).join(' › ') + '**');
                console.log('');
                console.log('```');
                console.log((t.failureMessages||[]).join('\n').substring(0,500));
                console.log('```');
                console.log('');
              }
              console.log('</details>');
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere jest-results.json: ' + e.message); }
          TSTEOF
            fi
            # ── Coverage ──────────────────────────────────────────────
            if [ -f coverage/coverage-summary.json ]; then
              echo ""
              echo "## 📊 Coverage — $(basename $PWD)"
              node << 'COVEOF'
          try {
            const s = require('./coverage/coverage-summary.json');
            const ok='✅', ko='❌';
            const fmt  = m => (m.pct>=90?ok:ko)+' **'+m.pct+'%**';
            const fmt2 = m => (m.pct>=90?ok:ko)+' '+m.pct+'%';
            const t = s.total;
            console.log('### Totale');
            console.log('| Metrica | Copertura | Covered / Total |');
            console.log('|---|---|---|');
            for (const [k,m] of Object.entries(t)) {
              console.log('| '+k+' | '+fmt(m)+' | '+m.covered+' / '+m.total+' |');
            }
            const files = Object.entries(s).filter(([k])=>k!=='total');
            if (files.length) {
              console.log('');
              console.log('<details><summary>📁 Dettaglio per file</summary>');
              console.log('');
              console.log('| File | Stmt | Branch | Func | Lines |');
              console.log('|---|---|---|---|---|');
              for (const [file,m] of files) {
                const name = file.split('/').pop();
                console.log('| `'+name+'` | '+fmt2(m.statements)+' | '+fmt2(m.branches)+' | '+fmt2(m.functions)+' | '+fmt2(m.lines)+' |');
              }
              console.log('');
              console.log('</details>');
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere coverage-summary.json: ' + e.message); }
          COVEOF
            else
              echo ""
              echo "## 📊 Coverage"
              echo "❌ Report non disponibile — test falliti o non eseguiti"
            fi
            echo ""
            echo "> 📁 Scarica l\'artefatto **coverage-$(basename $PWD)** per: HTML coverage (lcov-report/index.html), report test (test-report.html), dettaglio audit (audit-report.json)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: coverage-jobcard
          path: jobcard/coverage/
          retention-days: 30

  test-v360:
    name: v360
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: v360
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: v360/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Security scan
        run: |
          mkdir -p coverage
          npm audit --json > coverage/audit-report.json 2>&1 || true
          npm audit --audit-level=high && touch .scan-ok || true
      - name: Run tests with coverage (≥90%)
        run: npm run test:coverage -- --verbose
      - name: Generate step summary
        if: always()
        run: |
          {
            # ── Security Scan ─────────────────────────────────────────
            echo "## 🔒 Security Scan"
            if [ -f .scan-ok ]; then
              echo "✅ Nessuna vulnerabilità high/critical trovata"
            else
              echo "❌ Vulnerabilità high/critical rilevate"
            fi
            rm -f .scan-ok
            if [ -f coverage/audit-report.json ]; then
              node << 'AUDEOF'
          try {
            const a = require('./coverage/audit-report.json');
            const v = a.metadata?.vulnerabilities || {};
            const total = (v.info||0)+(v.low||0)+(v.moderate||0)+(v.high||0)+(v.critical||0);
            if (total > 0) {
              console.log('');
              console.log('| Severity | Conteggio |');
              console.log('|---|---|');
              if (v.critical) console.log('| 🔴 Critical | ' + v.critical + ' |');
              if (v.high)     console.log('| 🟠 High     | ' + v.high + ' |');
              if (v.moderate) console.log('| 🟡 Moderate | ' + v.moderate + ' |');
              if (v.low)      console.log('| 🟢 Low      | ' + v.low + ' |');
              if (v.info)     console.log('| ℹ️ Info    | ' + v.info + ' |');
              const highs = Object.values(a.vulnerabilities||{}).filter(p=>['high','critical'].includes(p.severity));
              if (highs.length) {
                console.log('');
                console.log('<details><summary>Dettaglio vulnerabilità high/critical</summary>');
                console.log('');
                console.log('| Package | Severity | Problema | Fix disponibile |');
                console.log('|---|---|---|---|');
                for (const p of highs) {
                  const via = (p.via||[]).map(v=>typeof v==='string'?v:(v.title||v.name||'?')).join(', ');
                  const fix = p.fixAvailable===true ? '✅ sì' : p.fixAvailable?.name ? '⚠️ breaking: '+p.fixAvailable.name : '❌ no';
                  console.log('| `'+p.name+'` | **'+p.severity+'** | '+via+' | '+fix+' |');
                }
                console.log('');
                console.log('</details>');
              }
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere audit-report.json: ' + e.message); }
          AUDEOF
            fi
            # ── Test Results ──────────────────────────────────────────
            if [ -f coverage/jest-results.json ]; then
              echo ""
              echo "## 🧪 Test Results — $(basename $PWD)"
              node << 'TSTEOF'
          try {
            const r = require('./coverage/jest-results.json');
            const ok='✅', ko='❌', sk='⏭️';
            console.log('| | Totale |');
            console.log('|---|---|');
            console.log('| ✅ Passed  | **' + r.numPassedTests  + '** |');
            if (r.numFailedTests)  console.log('| ❌ Failed  | **' + r.numFailedTests  + '** |');
            if (r.numPendingTests) console.log('| ⏭️ Skipped | **' + r.numPendingTests + '** |');
            console.log('| ⏱️ Durata  | ' + (r.testResults?.reduce((a,s)=>a+(s.endTime-s.startTime),0)/1000).toFixed(2) + 's |');
            const failed = (r.testResults||[]).flatMap(s=>(s.testResults||[]).filter(t=>t.status==='failed'));
            if (failed.length) {
              console.log('');
              console.log('<details><summary>❌ Test falliti</summary>');
              console.log('');
              for (const t of failed) {
                console.log('**' + t.ancestorTitles.concat(t.title).join(' › ') + '**');
                console.log('');
                console.log('```');
                console.log((t.failureMessages||[]).join('\n').substring(0,500));
                console.log('```');
                console.log('');
              }
              console.log('</details>');
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere jest-results.json: ' + e.message); }
          TSTEOF
            fi
            # ── Coverage ──────────────────────────────────────────────
            if [ -f coverage/coverage-summary.json ]; then
              echo ""
              echo "## 📊 Coverage — $(basename $PWD)"
              node << 'COVEOF'
          try {
            const s = require('./coverage/coverage-summary.json');
            const ok='✅', ko='❌';
            const fmt  = m => (m.pct>=90?ok:ko)+' **'+m.pct+'%**';
            const fmt2 = m => (m.pct>=90?ok:ko)+' '+m.pct+'%';
            const t = s.total;
            console.log('### Totale');
            console.log('| Metrica | Copertura | Covered / Total |');
            console.log('|---|---|---|');
            for (const [k,m] of Object.entries(t)) {
              console.log('| '+k+' | '+fmt(m)+' | '+m.covered+' / '+m.total+' |');
            }
            const files = Object.entries(s).filter(([k])=>k!=='total');
            if (files.length) {
              console.log('');
              console.log('<details><summary>📁 Dettaglio per file</summary>');
              console.log('');
              console.log('| File | Stmt | Branch | Func | Lines |');
              console.log('|---|---|---|---|---|');
              for (const [file,m] of files) {
                const name = file.split('/').pop();
                console.log('| `'+name+'` | '+fmt2(m.statements)+' | '+fmt2(m.branches)+' | '+fmt2(m.functions)+' | '+fmt2(m.lines)+' |');
              }
              console.log('');
              console.log('</details>');
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere coverage-summary.json: ' + e.message); }
          COVEOF
            else
              echo ""
              echo "## 📊 Coverage"
              echo "❌ Report non disponibile — test falliti o non eseguiti"
            fi
            echo ""
            echo "> 📁 Scarica l\'artefatto **coverage-$(basename $PWD)** per: HTML coverage (lcov-report/index.html), report test (test-report.html), dettaglio audit (audit-report.json)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: coverage-v360
          path: v360/coverage/
          retention-days: 30

  test-pkEper:
    name: pkEper
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: pkEper
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: pkEper/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Security scan
        run: |
          mkdir -p coverage
          npm audit --json > coverage/audit-report.json 2>&1 || true
          npm audit --audit-level=high && touch .scan-ok || true
      - name: Run tests with coverage (≥90%)
        run: npm run test:coverage -- --verbose
      - name: Generate step summary
        if: always()
        run: |
          {
            # ── Security Scan ─────────────────────────────────────────
            echo "## 🔒 Security Scan"
            if [ -f .scan-ok ]; then
              echo "✅ Nessuna vulnerabilità high/critical trovata"
            else
              echo "❌ Vulnerabilità high/critical rilevate"
            fi
            rm -f .scan-ok
            if [ -f coverage/audit-report.json ]; then
              node << 'AUDEOF'
          try {
            const a = require('./coverage/audit-report.json');
            const v = a.metadata?.vulnerabilities || {};
            const total = (v.info||0)+(v.low||0)+(v.moderate||0)+(v.high||0)+(v.critical||0);
            if (total > 0) {
              console.log('');
              console.log('| Severity | Conteggio |');
              console.log('|---|---|');
              if (v.critical) console.log('| 🔴 Critical | ' + v.critical + ' |');
              if (v.high)     console.log('| 🟠 High     | ' + v.high + ' |');
              if (v.moderate) console.log('| 🟡 Moderate | ' + v.moderate + ' |');
              if (v.low)      console.log('| 🟢 Low      | ' + v.low + ' |');
              if (v.info)     console.log('| ℹ️ Info    | ' + v.info + ' |');
              const highs = Object.values(a.vulnerabilities||{}).filter(p=>['high','critical'].includes(p.severity));
              if (highs.length) {
                console.log('');
                console.log('<details><summary>Dettaglio vulnerabilità high/critical</summary>');
                console.log('');
                console.log('| Package | Severity | Problema | Fix disponibile |');
                console.log('|---|---|---|---|');
                for (const p of highs) {
                  const via = (p.via||[]).map(v=>typeof v==='string'?v:(v.title||v.name||'?')).join(', ');
                  const fix = p.fixAvailable===true ? '✅ sì' : p.fixAvailable?.name ? '⚠️ breaking: '+p.fixAvailable.name : '❌ no';
                  console.log('| `'+p.name+'` | **'+p.severity+'** | '+via+' | '+fix+' |');
                }
                console.log('');
                console.log('</details>');
              }
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere audit-report.json: ' + e.message); }
          AUDEOF
            fi
            # ── Test Results ──────────────────────────────────────────
            if [ -f coverage/jest-results.json ]; then
              echo ""
              echo "## 🧪 Test Results — $(basename $PWD)"
              node << 'TSTEOF'
          try {
            const r = require('./coverage/jest-results.json');
            const ok='✅', ko='❌', sk='⏭️';
            console.log('| | Totale |');
            console.log('|---|---|');
            console.log('| ✅ Passed  | **' + r.numPassedTests  + '** |');
            if (r.numFailedTests)  console.log('| ❌ Failed  | **' + r.numFailedTests  + '** |');
            if (r.numPendingTests) console.log('| ⏭️ Skipped | **' + r.numPendingTests + '** |');
            console.log('| ⏱️ Durata  | ' + (r.testResults?.reduce((a,s)=>a+(s.endTime-s.startTime),0)/1000).toFixed(2) + 's |');
            const failed = (r.testResults||[]).flatMap(s=>(s.testResults||[]).filter(t=>t.status==='failed'));
            if (failed.length) {
              console.log('');
              console.log('<details><summary>❌ Test falliti</summary>');
              console.log('');
              for (const t of failed) {
                console.log('**' + t.ancestorTitles.concat(t.title).join(' › ') + '**');
                console.log('');
                console.log('```');
                console.log((t.failureMessages||[]).join('\n').substring(0,500));
                console.log('```');
                console.log('');
              }
              console.log('</details>');
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere jest-results.json: ' + e.message); }
          TSTEOF
            fi
            # ── Coverage ──────────────────────────────────────────────
            if [ -f coverage/coverage-summary.json ]; then
              echo ""
              echo "## 📊 Coverage — $(basename $PWD)"
              node << 'COVEOF'
          try {
            const s = require('./coverage/coverage-summary.json');
            const ok='✅', ko='❌';
            const fmt  = m => (m.pct>=90?ok:ko)+' **'+m.pct+'%**';
            const fmt2 = m => (m.pct>=90?ok:ko)+' '+m.pct+'%';
            const t = s.total;
            console.log('### Totale');
            console.log('| Metrica | Copertura | Covered / Total |');
            console.log('|---|---|---|');
            for (const [k,m] of Object.entries(t)) {
              console.log('| '+k+' | '+fmt(m)+' | '+m.covered+' / '+m.total+' |');
            }
            const files = Object.entries(s).filter(([k])=>k!=='total');
            if (files.length) {
              console.log('');
              console.log('<details><summary>📁 Dettaglio per file</summary>');
              console.log('');
              console.log('| File | Stmt | Branch | Func | Lines |');
              console.log('|---|---|---|---|---|');
              for (const [file,m] of files) {
                const name = file.split('/').pop();
                console.log('| `'+name+'` | '+fmt2(m.statements)+' | '+fmt2(m.branches)+' | '+fmt2(m.functions)+' | '+fmt2(m.lines)+' |');
              }
              console.log('');
              console.log('</details>');
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere coverage-summary.json: ' + e.message); }
          COVEOF
            else
              echo ""
              echo "## 📊 Coverage"
              echo "❌ Report non disponibile — test falliti o non eseguiti"
            fi
            echo ""
            echo "> 📁 Scarica l\'artefatto **coverage-$(basename $PWD)** per: HTML coverage (lcov-report/index.html), report test (test-report.html), dettaglio audit (audit-report.json)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: coverage-pkEper
          path: pkEper/coverage/
          retention-days: 30

  test-pkDocsoa:
    name: pkDocsoa
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: pkDocsoa
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: pkDocsoa/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Security scan
        run: |
          mkdir -p coverage
          npm audit --json > coverage/audit-report.json 2>&1 || true
          npm audit --audit-level=high && touch .scan-ok || true
      - name: Run tests with coverage (≥90%)
        run: npm run test:coverage -- --verbose
      - name: Generate step summary
        if: always()
        run: |
          {
            # ── Security Scan ─────────────────────────────────────────
            echo "## 🔒 Security Scan"
            if [ -f .scan-ok ]; then
              echo "✅ Nessuna vulnerabilità high/critical trovata"
            else
              echo "❌ Vulnerabilità high/critical rilevate"
            fi
            rm -f .scan-ok
            if [ -f coverage/audit-report.json ]; then
              node << 'AUDEOF'
          try {
            const a = require('./coverage/audit-report.json');
            const v = a.metadata?.vulnerabilities || {};
            const total = (v.info||0)+(v.low||0)+(v.moderate||0)+(v.high||0)+(v.critical||0);
            if (total > 0) {
              console.log('');
              console.log('| Severity | Conteggio |');
              console.log('|---|---|');
              if (v.critical) console.log('| 🔴 Critical | ' + v.critical + ' |');
              if (v.high)     console.log('| 🟠 High     | ' + v.high + ' |');
              if (v.moderate) console.log('| 🟡 Moderate | ' + v.moderate + ' |');
              if (v.low)      console.log('| 🟢 Low      | ' + v.low + ' |');
              if (v.info)     console.log('| ℹ️ Info    | ' + v.info + ' |');
              const highs = Object.values(a.vulnerabilities||{}).filter(p=>['high','critical'].includes(p.severity));
              if (highs.length) {
                console.log('');
                console.log('<details><summary>Dettaglio vulnerabilità high/critical</summary>');
                console.log('');
                console.log('| Package | Severity | Problema | Fix disponibile |');
                console.log('|---|---|---|---|');
                for (const p of highs) {
                  const via = (p.via||[]).map(v=>typeof v==='string'?v:(v.title||v.name||'?')).join(', ');
                  const fix = p.fixAvailable===true ? '✅ sì' : p.fixAvailable?.name ? '⚠️ breaking: '+p.fixAvailable.name : '❌ no';
                  console.log('| `'+p.name+'` | **'+p.severity+'** | '+via+' | '+fix+' |');
                }
                console.log('');
                console.log('</details>');
              }
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere audit-report.json: ' + e.message); }
          AUDEOF
            fi
            # ── Test Results ──────────────────────────────────────────
            if [ -f coverage/jest-results.json ]; then
              echo ""
              echo "## 🧪 Test Results — $(basename $PWD)"
              node << 'TSTEOF'
          try {
            const r = require('./coverage/jest-results.json');
            const ok='✅', ko='❌', sk='⏭️';
            console.log('| | Totale |');
            console.log('|---|---|');
            console.log('| ✅ Passed  | **' + r.numPassedTests  + '** |');
            if (r.numFailedTests)  console.log('| ❌ Failed  | **' + r.numFailedTests  + '** |');
            if (r.numPendingTests) console.log('| ⏭️ Skipped | **' + r.numPendingTests + '** |');
            console.log('| ⏱️ Durata  | ' + (r.testResults?.reduce((a,s)=>a+(s.endTime-s.startTime),0)/1000).toFixed(2) + 's |');
            const failed = (r.testResults||[]).flatMap(s=>(s.testResults||[]).filter(t=>t.status==='failed'));
            if (failed.length) {
              console.log('');
              console.log('<details><summary>❌ Test falliti</summary>');
              console.log('');
              for (const t of failed) {
                console.log('**' + t.ancestorTitles.concat(t.title).join(' › ') + '**');
                console.log('');
                console.log('```');
                console.log((t.failureMessages||[]).join('\n').substring(0,500));
                console.log('```');
                console.log('');
              }
              console.log('</details>');
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere jest-results.json: ' + e.message); }
          TSTEOF
            fi
            # ── Coverage ──────────────────────────────────────────────
            if [ -f coverage/coverage-summary.json ]; then
              echo ""
              echo "## 📊 Coverage — $(basename $PWD)"
              node << 'COVEOF'
          try {
            const s = require('./coverage/coverage-summary.json');
            const ok='✅', ko='❌';
            const fmt  = m => (m.pct>=90?ok:ko)+' **'+m.pct+'%**';
            const fmt2 = m => (m.pct>=90?ok:ko)+' '+m.pct+'%';
            const t = s.total;
            console.log('### Totale');
            console.log('| Metrica | Copertura | Covered / Total |');
            console.log('|---|---|---|');
            for (const [k,m] of Object.entries(t)) {
              console.log('| '+k+' | '+fmt(m)+' | '+m.covered+' / '+m.total+' |');
            }
            const files = Object.entries(s).filter(([k])=>k!=='total');
            if (files.length) {
              console.log('');
              console.log('<details><summary>📁 Dettaglio per file</summary>');
              console.log('');
              console.log('| File | Stmt | Branch | Func | Lines |');
              console.log('|---|---|---|---|---|');
              for (const [file,m] of files) {
                const name = file.split('/').pop();
                console.log('| `'+name+'` | '+fmt2(m.statements)+' | '+fmt2(m.branches)+' | '+fmt2(m.functions)+' | '+fmt2(m.lines)+' |');
              }
              console.log('');
              console.log('</details>');
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere coverage-summary.json: ' + e.message); }
          COVEOF
            else
              echo ""
              echo "## 📊 Coverage"
              echo "❌ Report non disponibile — test falliti o non eseguiti"
            fi
            echo ""
            echo "> 📁 Scarica l\'artefatto **coverage-$(basename $PWD)** per: HTML coverage (lcov-report/index.html), report test (test-report.html), dettaglio audit (audit-report.json)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: coverage-pkDocsoa
          path: pkDocsoa/coverage/
          retention-days: 30

  test-pkMenupricing:
    name: pkMenupricing
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: pkMenupricing
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: pkMenupricing/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Security scan
        run: |
          mkdir -p coverage
          npm audit --json > coverage/audit-report.json 2>&1 || true
          npm audit --audit-level=high && touch .scan-ok || true
      - name: Run tests with coverage (≥90%)
        run: npm run test:coverage -- --verbose
      - name: Generate step summary
        if: always()
        run: |
          {
            # ── Security Scan ─────────────────────────────────────────
            echo "## 🔒 Security Scan"
            if [ -f .scan-ok ]; then
              echo "✅ Nessuna vulnerabilità high/critical trovata"
            else
              echo "❌ Vulnerabilità high/critical rilevate"
            fi
            rm -f .scan-ok
            if [ -f coverage/audit-report.json ]; then
              node << 'AUDEOF'
          try {
            const a = require('./coverage/audit-report.json');
            const v = a.metadata?.vulnerabilities || {};
            const total = (v.info||0)+(v.low||0)+(v.moderate||0)+(v.high||0)+(v.critical||0);
            if (total > 0) {
              console.log('');
              console.log('| Severity | Conteggio |');
              console.log('|---|---|');
              if (v.critical) console.log('| 🔴 Critical | ' + v.critical + ' |');
              if (v.high)     console.log('| 🟠 High     | ' + v.high + ' |');
              if (v.moderate) console.log('| 🟡 Moderate | ' + v.moderate + ' |');
              if (v.low)      console.log('| 🟢 Low      | ' + v.low + ' |');
              if (v.info)     console.log('| ℹ️ Info    | ' + v.info + ' |');
              const highs = Object.values(a.vulnerabilities||{}).filter(p=>['high','critical'].includes(p.severity));
              if (highs.length) {
                console.log('');
                console.log('<details><summary>Dettaglio vulnerabilità high/critical</summary>');
                console.log('');
                console.log('| Package | Severity | Problema | Fix disponibile |');
                console.log('|---|---|---|---|');
                for (const p of highs) {
                  const via = (p.via||[]).map(v=>typeof v==='string'?v:(v.title||v.name||'?')).join(', ');
                  const fix = p.fixAvailable===true ? '✅ sì' : p.fixAvailable?.name ? '⚠️ breaking: '+p.fixAvailable.name : '❌ no';
                  console.log('| `'+p.name+'` | **'+p.severity+'** | '+via+' | '+fix+' |');
                }
                console.log('');
                console.log('</details>');
              }
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere audit-report.json: ' + e.message); }
          AUDEOF
            fi
            # ── Test Results ──────────────────────────────────────────
            if [ -f coverage/jest-results.json ]; then
              echo ""
              echo "## 🧪 Test Results — $(basename $PWD)"
              node << 'TSTEOF'
          try {
            const r = require('./coverage/jest-results.json');
            const ok='✅', ko='❌', sk='⏭️';
            console.log('| | Totale |');
            console.log('|---|---|');
            console.log('| ✅ Passed  | **' + r.numPassedTests  + '** |');
            if (r.numFailedTests)  console.log('| ❌ Failed  | **' + r.numFailedTests  + '** |');
            if (r.numPendingTests) console.log('| ⏭️ Skipped | **' + r.numPendingTests + '** |');
            console.log('| ⏱️ Durata  | ' + (r.testResults?.reduce((a,s)=>a+(s.endTime-s.startTime),0)/1000).toFixed(2) + 's |');
            const failed = (r.testResults||[]).flatMap(s=>(s.testResults||[]).filter(t=>t.status==='failed'));
            if (failed.length) {
              console.log('');
              console.log('<details><summary>❌ Test falliti</summary>');
              console.log('');
              for (const t of failed) {
                console.log('**' + t.ancestorTitles.concat(t.title).join(' › ') + '**');
                console.log('');
                console.log('```');
                console.log((t.failureMessages||[]).join('\n').substring(0,500));
                console.log('```');
                console.log('');
              }
              console.log('</details>');
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere jest-results.json: ' + e.message); }
          TSTEOF
            fi
            # ── Coverage ──────────────────────────────────────────────
            if [ -f coverage/coverage-summary.json ]; then
              echo ""
              echo "## 📊 Coverage — $(basename $PWD)"
              node << 'COVEOF'
          try {
            const s = require('./coverage/coverage-summary.json');
            const ok='✅', ko='❌';
            const fmt  = m => (m.pct>=90?ok:ko)+' **'+m.pct+'%**';
            const fmt2 = m => (m.pct>=90?ok:ko)+' '+m.pct+'%';
            const t = s.total;
            console.log('### Totale');
            console.log('| Metrica | Copertura | Covered / Total |');
            console.log('|---|---|---|');
            for (const [k,m] of Object.entries(t)) {
              console.log('| '+k+' | '+fmt(m)+' | '+m.covered+' / '+m.total+' |');
            }
            const files = Object.entries(s).filter(([k])=>k!=='total');
            if (files.length) {
              console.log('');
              console.log('<details><summary>📁 Dettaglio per file</summary>');
              console.log('');
              console.log('| File | Stmt | Branch | Func | Lines |');
              console.log('|---|---|---|---|---|');
              for (const [file,m] of files) {
                const name = file.split('/').pop();
                console.log('| `'+name+'` | '+fmt2(m.statements)+' | '+fmt2(m.branches)+' | '+fmt2(m.functions)+' | '+fmt2(m.lines)+' |');
              }
              console.log('');
              console.log('</details>');
            }
          } catch(e) { console.log('> ⚠️ impossibile leggere coverage-summary.json: ' + e.message); }
          COVEOF
            else
              echo ""
              echo "## 📊 Coverage"
              echo "❌ Report non disponibile — test falliti o non eseguiti"
            fi
            echo ""
            echo "> 📁 Scarica l\'artefatto **coverage-$(basename $PWD)** per: HTML coverage (lcov-report/index.html), report test (test-report.html), dettaglio audit (audit-report.json)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: coverage-pkMenupricing
          path: pkMenupricing/coverage/
          retention-days: 30


  # ── Deploy jobs ────────────────────────────────────────────────────────────────
  # Branch → AWS environment:
  #   develop    → dev   (push diretto)
  #   staging    → stage (push diretto o merge da develop)
  #   production → prod  (push diretto o merge da staging)
  #
  # dev e stage condividono le stesse variabili (repository-level secrets/vars):
  #   vars:    AWS_ROLE_ARN, S3_BUCKET
  #   secrets: VPC_SUBNET_IDS, VPC_SECURITY_GROUP_IDS, PING_CLIENT_ID,
  #            PING_CLIENT_SECRET, AGENDA_SOA_HOST, AGENDA_SOA_USERNAME,
  #            AGENDA_SOA_PASSWORD, AGENDA_SOA_API_KEY, DML_IBM_CLIENT_ID,
  #            DML_IBM_CLIENT_SECRET, DGT_CLIENT_ID, DGT_CLIENT_SECRET,
  #            ASV_CLIENT_ID, ASV_CLIENT_SECRET
  #
  # prod usa GitHub Environment "prod" (Settings → Environments) con le proprie
  # variabili — da configurare quando disponibili.
  # Si consiglia di abilitare "Required reviewers" sull'environment prod.

  deploy-dev:
    name: Deploy to AWS (SAM) — dev
    needs:
      - test-agendaSoa
      - test-agendaSoaNaga
      - test-dms
      - test-jobcard
      - test-v360
    if: github.event_name == 'push' && github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "24"

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region:     eu-west-1

      - name: Install AWS SAM CLI
        uses: aws-actions/setup-sam@v3

      - name: Install npm dependencies (all modules)
        run: |
          (cd agendaSoa     && npm ci --omit=dev)
          (cd agendaSoaNaga && npm ci --omit=dev)
          (cd dms           && npm ci --omit=dev)
          (cd jobcard       && npm ci --omit=dev)
          (cd v360          && npm ci --omit=dev)

      - name: SAM Build
        run: sam build --parallel

      - name: SAM Deploy — dev
        run: |
          sam deploy \
            --stack-name stla-woc-backend-dev \
            --s3-bucket s3-np-bsn0027990-dev-ops \
            --s3-prefix stla-woc-backend \
            --region eu-west-1 \
            --capabilities CAPABILITY_IAM \
            --no-confirm-changeset \
            --no-fail-on-empty-changeset \
            --parameter-overrides \
              Environment=dev \
              VpcSubnetIds="${{ secrets.VPC_SUBNET_IDS }}" \
              VpcSecurityGroupIds="${{ secrets.VPC_SECURITY_GROUP_IDS }}" \
              PingClientId="${{ secrets.PING_CLIENT_ID }}" \
              PingClientSecret="${{ secrets.PING_CLIENT_SECRET }}" \
              AgendaSoaHost="${{ secrets.AGENDA_SOA_HOST }}" \
              AgendaSoaUsername="${{ secrets.AGENDA_SOA_USERNAME }}" \
              AgendaSoaPassword="${{ secrets.AGENDA_SOA_PASSWORD }}" \
              AgendaSoaApiKey="${{ secrets.AGENDA_SOA_API_KEY }}" \
              DmlIbmClientId="${{ secrets.DML_IBM_CLIENT_ID }}" \
              DmlIbmClientSecret="${{ secrets.DML_IBM_CLIENT_SECRET }}" \
              DgtClientId="${{ secrets.DGT_CLIENT_ID }}" \
              DgtClientSecret="${{ secrets.DGT_CLIENT_SECRET }}" \
              AsvClientId="${{ secrets.ASV_CLIENT_ID }}" \
              AsvClientSecret="${{ secrets.ASV_CLIENT_SECRET }}"

  deploy-stage:
    name: Deploy to AWS (SAM) — stage
    needs:
      - test-agendaSoa
      - test-agendaSoaNaga
      - test-dms
      - test-jobcard
      - test-v360
    if: github.event_name == 'push' && github.ref == 'refs/heads/staging'
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "24"

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region:     eu-west-1

      - name: Install AWS SAM CLI
        uses: aws-actions/setup-sam@v3

      - name: Install npm dependencies (all modules)
        run: |
          (cd agendaSoa     && npm ci --omit=dev)
          (cd agendaSoaNaga && npm ci --omit=dev)
          (cd dms           && npm ci --omit=dev)
          (cd jobcard       && npm ci --omit=dev)
          (cd v360          && npm ci --omit=dev)

      - name: SAM Build
        run: sam build --parallel

      - name: SAM Deploy — stage
        run: |
          sam deploy \
            --stack-name stla-woc-backend-stage \
            --s3-bucket s3-np-bsn0027990-dev-ops \
            --s3-prefix stla-woc-backend \
            --region eu-west-1 \
            --capabilities CAPABILITY_IAM \
            --no-confirm-changeset \
            --no-fail-on-empty-changeset \
            --parameter-overrides \
              Environment=stage \
              VpcSubnetIds="${{ secrets.VPC_SUBNET_IDS }}" \
              VpcSecurityGroupIds="${{ secrets.VPC_SECURITY_GROUP_IDS }}" \
              PingClientId="${{ secrets.PING_CLIENT_ID }}" \
              PingClientSecret="${{ secrets.PING_CLIENT_SECRET }}" \
              AgendaSoaHost="${{ secrets.AGENDA_SOA_HOST }}" \
              AgendaSoaUsername="${{ secrets.AGENDA_SOA_USERNAME }}" \
              AgendaSoaPassword="${{ secrets.AGENDA_SOA_PASSWORD }}" \
              AgendaSoaApiKey="${{ secrets.AGENDA_SOA_API_KEY }}" \
              DmlIbmClientId="${{ secrets.DML_IBM_CLIENT_ID }}" \
              DmlIbmClientSecret="${{ secrets.DML_IBM_CLIENT_SECRET }}" \
              DgtClientId="${{ secrets.DGT_CLIENT_ID }}" \
              DgtClientSecret="${{ secrets.DGT_CLIENT_SECRET }}" \
              AsvClientId="${{ secrets.ASV_CLIENT_ID }}" \
              AsvClientSecret="${{ secrets.ASV_CLIENT_SECRET }}"

  deploy-prod:
    name: Deploy to AWS (SAM) — prod
    needs:
      - test-agendaSoa
      - test-agendaSoaNaga
      - test-dms
      - test-jobcard
      - test-v360
    if: github.event_name == 'push' && github.ref == 'refs/heads/production'
    runs-on: ubuntu-latest
    environment: prod
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "24"

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region:     eu-west-1

      - name: Install AWS SAM CLI
        uses: aws-actions/setup-sam@v3

      - name: Install npm dependencies (all modules)
        run: |
          (cd agendaSoa     && npm ci --omit=dev)
          (cd agendaSoaNaga && npm ci --omit=dev)
          (cd dms           && npm ci --omit=dev)
          (cd jobcard       && npm ci --omit=dev)
          (cd v360          && npm ci --omit=dev)

      - name: SAM Build
        run: sam build --parallel

      - name: SAM Deploy — prod
        run: |
          sam deploy \
            --stack-name stla-woc-backend-prod \
            --s3-bucket ${{ vars.S3_BUCKET }} \
            --s3-prefix stla-woc-backend \
            --region eu-west-1 \
            --capabilities CAPABILITY_IAM \
            --no-confirm-changeset \
            --no-fail-on-empty-changeset \
            --parameter-overrides \
              Environment=prod \
              VpcSubnetIds="${{ secrets.VPC_SUBNET_IDS }}" \
              VpcSecurityGroupIds="${{ secrets.VPC_SECURITY_GROUP_IDS }}" \
              PingClientId="${{ secrets.PING_CLIENT_ID }}" \
              PingClientSecret="${{ secrets.PING_CLIENT_SECRET }}" \
              AgendaSoaHost="${{ secrets.AGENDA_SOA_HOST }}" \
              AgendaSoaUsername="${{ secrets.AGENDA_SOA_USERNAME }}" \
              AgendaSoaPassword="${{ secrets.AGENDA_SOA_PASSWORD }}" \
              AgendaSoaApiKey="${{ secrets.AGENDA_SOA_API_KEY }}" \
              DmlIbmClientId="${{ secrets.DML_IBM_CLIENT_ID }}" \
              DmlIbmClientSecret="${{ secrets.DML_IBM_CLIENT_SECRET }}" \
              DgtClientId="${{ secrets.DGT_CLIENT_ID }}" \
              DgtClientSecret="${{ secrets.DGT_CLIENT_SECRET }}" \
              AsvClientId="${{ secrets.ASV_CLIENT_ID }}" \
              AsvClientSecret="${{ secrets.ASV_CLIENT_SECRET }}"
