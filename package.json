{
  "name": "stellantis-woc-backend",
  "version": "1.0.0",
  "description": "Stellantis WOC Backend - AWS Lambda REST APIs",
  "scripts": {
    "test": "jest --coverage synch-status/__tests__",
    "test:watch": "jest --watch synch-status/__tests__",
    "test:unit": "jest synch-status/__tests__/unit --coverage",
    "lint": "eslint synch-status/*.js",
    "install:lambda": "cd synch-status && npm install && cd .."
  },
  "dependencies": {},
  "devDependencies": {
    "jest": "^29.7.0",
    "eslint": "^8.57.0",
    "@jest/globals": "^29.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
